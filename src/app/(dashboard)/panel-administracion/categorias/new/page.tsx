import React from "react";
import CreationForm from "./_components/CreationForm/form";

const Page = () => {
  return (
    <main className="w-full min-h-screen">
      <section className="w-full h-full flex items-center justify-center">
        <CreationForm />
      </section>
    </main>
  );
};

export default Page;
